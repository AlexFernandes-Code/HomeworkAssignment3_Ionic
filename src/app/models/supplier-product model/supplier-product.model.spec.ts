import { SupplierProduct } from './supplier-product.model';

describe('SupplierProduct.Model', () => {
  it('should create an instance', () => {
    expect(new SupplierProduct()).toBeTruthy();
  });
});
